{
  "name": "analystt",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --scope=web-app",
    "build": "lerna run build --scope=web-app"
  },
  "devDependencies": {
    "lerna": "^7.4.2"
  }
}
